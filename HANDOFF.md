# 다음 작업 — 산안법 계열 행정규칙 34건 추가

> 2026-09-04 후속 수정: 전체 줄바꿈과 표 폭을 점검한다.
> 고시 16건·조문 41곳에서 문장 끝의 「다.」를 목으로 잘못 자른 문제를 원문과 대조해 복구했다.
> 별표 197건의 표시를 개선했다. 확인 가능한 문맥으로 인쇄용 줄을 잇고, 혼합 테두리·제목에 붙은 테두리를 인식한다.
> 구조가 불명확한 표는 원문으로 보존한다. 본문은 단어 단위로 줄을 바꾸고, 조문 링크는 중간에서 끊지 않는다.
> 320·390·768px에서 조문 2,751개·별표 620개 전부를 열어 화면 넘침·과도하게 좁은 표 열이 없음을 확인했다.
> `verify.py`: 파서·원문 보존 11개와 브라우저 177개 모두 통과. 별표 620건의 글자·숫자·기호도 전수 대조한다.

> 2026-09-04 후속 수정: 번호 없는 「고용노동부령」「대통령령」 위임 문구 335곳을 연결한다.
> 국가법령정보센터 lsDelegated API의 항·호·문맥에 맞춰 하위 조문을 지정한다.
> 대상이 하나면 바로 열고, 여러 개면 검색 가능한 위임 조문 목록을 연다. 뒤로가기로 목록과 검색어를 복원한다.
> `scripts/delegation.py`가 수집·검증하고 `data/delegations.json`에 출처와 원문 해시를 보관한다.
> `fetch_laws.py`로 법령을 갱신하면 연결 자료도 다시 받는다. 원문과 자료가 어긋나면 빌드를 멈춘다.
> `verify.py`에서 파서 검사 5개와 브라우저 검사 170개가 모두 통과했다.

> 2026-09-04 후속 수정: 기타 탭 맨 위에 「기초 법지식」을 추가한다.
> 법령·행정규칙의 종류와 과태료·사법조치를 설명하고, 벌칙 모아보기의 개념 설명은 이곳으로 옮긴다.
> 출처와 앱 속 근거 조문을 연결했다. 메뉴 진입·뒤로가기 등을 포함한 스모크 검사 156개가 모두 통과했다.

> 2026-09-04 후속 수정: 제목 앞 배지를 원문의 고시·훈령·예규로 표시하고,
> 취업제한규칙은 부령으로 표시한다. 지침 필터와 단계 6의 검색 우선순위는 유지한다.
> `python scripts/verify.py`: 배지 종류 검사를 포함해 148개 모두 통과했다.

> 2026-09-04 구현 완료: 행정규칙 34건을 추가해 총 66건(조문 2,751개·별표 620개)을 담았다.
> 지침 17건의 배지·필터·단계 6을 추가하고 web/·dist/를 다시 구웠다.
> `python scripts/verify.py`: 145개 모두 통과. 「추락」 검색은 기준규칙 제42조 1위, 첫 지침 39위다.
> 법령 데이터 압축 1.96MB, PWA 전체 2.76MB, 단일 HTML 2.71MB다.
> 아래는 작업 당시의 계획이다. 당시 파일 수는 26건이 아니라 25건이었으며,
> 추가 후 data/notices/는 59건, 법령 7건을 합친 앱 수록 총수는 66건이다.

## 무엇을

국가법령정보센터의 고용노동부 **현행** 행정규칙 438건을 전부 훑어 산업안전보건법에
근거한 것 74건을 가려냈다. 앱에는 26건만 들어 있어 48건이 빠져 있었다.
그중 **34건**을 넣는다. 나머지(기관 내부 운영·행정 12건, 타 부처 소관 1건,
「고기압 작업에 관한 기준」 1건)는 넣지 않기로 정했다.

「고기압 작업에 관한 기준」은 28만 자인데 잠함·잠수작업에만 쓰여 뺐다.

## A. 고시로 넣을 것 — 17건

`scripts/fetch_laws.py` 의 `NOTICES` 에 추가한다. 배지는 기본값 「고시」가 붙는다.

```
건설공사 안전보건대장의 작성 등에 관한 고시
건설업 유해·위험방지계획서 중 지도사가 평가·확인 할 수 있는 대상 건설공사의 범위 및 지도사의 요건
건설업체의 산업재해예방활동 실적 평가기준
근로자 건강증진활동 지침
근로자 건강진단 관리규정
명예산업안전감독관 운영규정
사무실 공기관리 지침
산업보건의 관리규정
산업재해통계업무처리규정
석면해체·제거업자 종사인력의 교육에 관한 규정
안전·보건관리전문기관 및 건설재해예방전문지도기관 관리규정
안전·보건에 관한 업무 수행시간의 기준 고시
영상표시단말기(VDT) 취급근로자 작업관리지침
외국어로 작성하는 안전보건표지에 관한 규정
위험기계·기구 자율안전확인 고시
유해·위험방지계획서 자체심사 및 확인업체 지정대상 건설업체 고시
재해원인조사 및 재해조사보고서 운영에 관한 규정
```

「위험기계·기구 자율안전확인 고시」는 62만 자로 혼자 A의 절반이 넘는다. 앱에 이미
든 「위험기계·기구 안전인증 고시」와 짝이라 넣는다 — 방호장치·보호구는 안전인증과
자율안전이 짝으로 들어 있는데 위험기계·기구만 한쪽이 비어 있었다.

## B. 지침으로 넣을 것 — 17건

같은 `NOTICES` 에 넣되, **배지와 묶음을 따로 준다**(아래 3번).

```
가설공사 표준안전 작업지침
가스누출감지경보기 설치에 관한 기술상의 지침
감전재해 예방을 위한 기술상의 지침
공작기계 안전기준 일반에 관한 기술상의 지침
굴착공사 표준안전 작업지침
발파 표준안전 작업지침
벌목 표준안전 작업지침
운반하역 표준안전 작업지침
저압산업용기계기구의 부속전기설비의 전기재해 예방을 위한 기술상의 지침
정전기재해 예방을 위한 기술상의 지침
제1차 금속산업 안전작업지침
철강업에 있어서 수증기 폭발 및 고열물 접촉위험 방지를 위한 기술상의 지침
철골공사표준안전작업지침
추락재해방지표준안전작업지침
콘크리트공사 표준안전 작업지침
터널공사 표준안전 작업지침-NATM공법
해체공사표준안전작업지침
```

## 어떻게

### 1. `scripts/fetch_laws.py`

`NOTICES` 에 위 34개 이름을 넣는다. 이 목록의 **차례가 곧 앱의 목차 차례**이므로
주제끼리 모아 둔다(건설·보건·기계 등). B 17건은 끝에 몰아 둔다.

이름은 국가법령정보센터 표기 그대로여야 한다. `search()` 가 이름으로 정확히
맞춰 찾고, 못 찾으면 `SystemExit` 으로 멈춘다.

### 2. 원문 받기

```bash
python scripts/fetch_laws.py
```

`data/notices/` 에 34개 파일이 새로 생긴다. 60건이 되어야 한다.

### 3. `scripts/build_app.py` — B 를 「지침」으로 가른다

`NOTICE_LEVELS` 에 B 17건을 넣는다. 값은 `("지침", 6, "지침")`.

```python
NOTICE_LEVELS = {
    norm("근로감독관 집무규정(산업안전보건)"): ("감독규정", 5, "감독규정"),
    norm("가설공사 표준안전 작업지침"): ("지침", 6, "지침"),
    ...   # B 17건 모두
}
```

`군` 이 「지침」이 되면 검색 화면 위의 필터 칩에 「지침」이 저절로 생긴다
(`start()` 가 `DOCS.map(d => d.군)` 으로 칩을 만든다).

### 4. `scripts/app_template.html` — 단계 6 을 만든다

**지금 단계는 5까지만 있다.** 6 을 쓰려면 두 곳을 손봐야 한다.

- 색 변수 `--t6` 를 밝기 세 벌(기본·다크 미디어쿼리·`[data-theme=dark]`)에 모두 더한다.
  `--t5` 보다 한 단계 흐린 값이면 된다.
- `.badge.l6{background:var(--t6)}` 를 `.badge.l5` 옆에 더한다.

단계 6 은 검색 점수에도 쓰인다 — `_score` 가 `단계 * 2` 를 빼므로, 지침은 고시보다
2점 낮아 조문·고시가 먼저 나온다. 이것이 B 를 따로 가르는 목적이다.

### 5. 굽기

```bash
python scripts/build_pwa.py     # web/
python scripts/build_app.py     # dist/
```

원문 12.1MB → 15.5MB, 내려받기 1.63MB → **1.85MB 안팎**으로 늘 것으로 실측했다.
크게 벗어나면 무언가 잘못된 것이다.

### 6. 검사

`scripts/smoke_test.js` 는 현재 **134개 항목이 모두 통과**한다. 법령 수를 박아 둔
검사가 있어 반드시 고쳐야 한다.

- `ok("법령 32건 적재", ... DOCS.length === 32 ...)` → **66** 으로
- `ok("목차: 법령 목록", ... >= 29 ...)` → 알맞게
- `ok("색인 2641건 내외", ... > 2500 ...)` → 조문이 늘었으니 확인

돌리는 법 — `python scripts/verify.py` 가 크롬을 화면 없이 띄워 전부 눌러 본다.
로컬에서 눈으로 볼 때는 `python scripts/preview.py`.

새로 넣을 검사도 하나 있으면 좋다: **지침이 고시보다 검색 순위가 낮은지**.

### 7. 확인할 것

- 「추락」으로 찾았을 때 **기준규칙 제42조가 지침보다 위**에 나오는지
- 목차 탭에 「지침」 배지가 고시와 다른 색으로 보이는지
- 필터 칩에 「지침」이 생겼는지
- 위험기계·기구 안전인증 고시와 자율안전확인 고시가 **나란히** 있는지

## 저장소 규칙

- `web/` 과 `dist/` 는 **반드시 다시 구워 함께 커밋**한다. 소스만 고치면 저장소와
  실제 앱이 어긋난다(GitHub 웹에서 고칠 때 특히 자주 어긋났다).
- 커밋 글은 「~한다」체로 **무엇을 왜 바꿨는지**를 적는다. 파일 이름만 적지 않는다.
- `main` 에 바로 커밋·푸시한다. 푸시하면 GitHub Actions 가 굽고 검사한 뒤
  Pages 에 올린다. 검사에서 하나라도 FAIL 이면 배포가 멈춘다.
- 커밋 글 끝에 다음 줄을 붙인다.

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

## 참고 — 이 저장소의 구조

```
data/laws/      법령 7건 (법·령·칙·기준규칙·취업제한규칙·중대재해법·령)
data/notices/   행정규칙 26건 → 60건이 된다
scripts/fetch_laws.py    국가법령정보 OPEN API 에서 원문 수집
scripts/build_app.py     LEVELS·NOTICE_LEVELS 로 배지·단계·묶음을 정하고 dist/ 를 굽는다
scripts/penalty.py       제12장 벌칙·과태료를 조문에 붙인다 (시행령 별표 35 에서 캔다)
scripts/app_template.html  앱 본체. web/ 과 dist/ 가 여기서 나온다
scripts/smoke_test.js    실제 브라우저에서 눌러 보는 검사 134개
scripts/verify.py        크롬을 화면 없이 띄워 smoke_test.js 를 돌린다
```

원문 수집은 `OC=test` 공용 샘플 계정을 쓴다. 34건을 한 번에 받으면 느리거나
막힐 수 있으니, 막히면 나누어 받거나 `LAW_API_OC` 환경변수에 본인 계정을 넣는다.
